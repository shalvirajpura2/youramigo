/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Allow server actions and edge compatibility
  },
  // Allow the app to be used without strict mode issues for demo
  reactStrictMode: false,
}

export default nextConfig
